import React, {Component} from 'react';

class Resolve extends Component {
    render() {
        return (
            <div>
                resolve component
            </div>
        );
    }
}

export default Resolve;